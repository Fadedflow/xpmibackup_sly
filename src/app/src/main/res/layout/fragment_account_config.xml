<?xml version="1.0" encoding="utf-8"?>
<!-- 云盘账号页：主 CTA + 账号卡片列表；页面标题由顶栏统一承担 -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/background"
    android:paddingStart="@dimen/page_padding_h"
    android:paddingEnd="@dimen/page_padding_h"
    android:paddingTop="@dimen/page_padding_top">

    <Button
        android:id="@+id/btn_add_cloud_account"
        style="@style/XmButtonPrimary"
        android:layout_marginBottom="@dimen/space_16"
        android:text="@string/add_cloud_account" />

    <TextView
        android:id="@+id/tv_cloud_empty"
        style="@style/XmTextBody"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="@dimen/space_32"
        android:text="@string/no_cloud_account_hint"
        android:textColor="@color/text_hint"
        android:visibility="gone" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:scrollbars="none">

        <LinearLayout
            android:id="@+id/cloud_account_list"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingBottom="@dimen/page_padding_bottom" />
    </ScrollView>
</LinearLayout>
